# Base de datos (SQLite local — no funciona en serverless, usar solo en desarrollo)
DATABASE_URL="file:./dev.db"

# NextAuth (si se usa en el futuro)
NEXTAUTH_SECRET="cambiar-esto-en-produccion"
NEXTAUTH_URL="http://localhost:3000"

# 🔒 Clave secreta para proteger el endpoint de subida de PDFs
# Si esta variable está definida, solo las peticiones con el header
# "x-upload-secret" correcto podrán subir archivos.
# Generá una clave segura con: openssl rand -hex 32
UPLOAD_SECRET="C@aldosivi13"
